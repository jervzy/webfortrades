# Resend transactional email API key (set in the Vercel project env; never commit the real key).
# Used by api/notify.js to send the internal lead email + customer confirmation.
RESEND_API_KEY=your_resend_api_key_here
